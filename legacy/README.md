# Legacy / MiroFish

Questa cartella contiene il progetto originale **MiroFish** (Python backend + Vite frontend)
da cui questa repo è stata forkata. Resta come **riferimento architetturale** per il nuovo
Event/Macro Studier in Node.js che vive nella root del repo.

- `README-mirofish.md` — README originale (cinese)
- `README-mirofish-EN.md` — README originale (inglese)
- `backend/` — backend Python (Flask + Zep GraphRAG + OASIS simulation)
- `frontend/` — frontend Vue/Vite originale
- `docker-compose.yml`, `Dockerfile` — deploy originale (non usato dal nuovo progetto)

Il nuovo progetto Node.js/Next.js è in `app/`, `lib/`, `supabase/`.
